# Nixpacks configuration for Backend API
# Nixpacks auto-detects Python and creates venv
# The venv is automatically activated in PATH

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
